{
  "name": "fluxiq",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "dev": "pnpm --filter @fluxiq/web dev",
    "build": "pnpm --filter @fluxiq/contracts build && pnpm --filter fluxiq build && pnpm --filter @fluxiq/client-gateway-websocket build && pnpm --filter @fluxiq/web build",
    "check": "pnpm -r check",
    "test": "pnpm -r test",
    "package:lint": "publint packages/contracts --strict && publint packages/fluxiq --strict && publint packages/client-gateway-websocket --strict && attw --pack --profile esm-only packages/contracts && attw --pack --profile esm-only packages/fluxiq && attw --pack --profile esm-only packages/client-gateway-websocket",
    "package:smoke": "node scripts/validate-packages.mjs",
    "package:validate": "pnpm build && pnpm package:lint && pnpm package:smoke"
  },
  "devDependencies": {
    "@arethetypeswrong/cli": "^0.18.2",
    "@types/node": "^22.10.0",
    "esbuild": "^0.25.0",
    "publint": "^0.3.12",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
