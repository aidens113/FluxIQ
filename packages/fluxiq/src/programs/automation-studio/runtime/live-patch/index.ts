// The runtime target override's check: what a domain is asked about a
// proposed target, the words a refusal is recorded in, and the judgement both
// the proposed and the executed path share. `../live-patch.ts` runs the patch;
// this decides whether a target override may be run or proposed at all.

export * from "./failed-action.ts";
export * from "./refusal-reasons.ts";
export * from "./target-override-check.ts";
